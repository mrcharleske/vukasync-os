# @vukasync/utils

Shared framework-neutral utilities for VukaSync OS.

## Current scope

The package currently exposes technical foundation utilities only. Business
logic should be added only when it is shared across apps and backed by agreed
product contracts.

## Commands

```bash
pnpm --filter @vukasync/utils typecheck
pnpm --filter @vukasync/utils build
```
