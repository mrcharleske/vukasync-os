{
  "name": "@vukasync/utils",
  "private": true,
  "version": "0.0.0",
  "description": "Shared framework-neutral utilities for VukaSync OS.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests configured for @vukasync/utils yet.\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@vukasync/types": "workspace:*"
  },
  "devDependencies": {
    "@vukasync/config": "workspace:*"
  }
}
