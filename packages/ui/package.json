{
  "name": "@vukasync/ui",
  "private": true,
  "version": "0.0.0",
  "description": "Shared design tokens and UI primitives for VukaSync OS.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests configured for @vukasync/ui yet.\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@vukasync/types": "workspace:*"
  },
  "devDependencies": {
    "@vukasync/config": "workspace:*"
  }
}
